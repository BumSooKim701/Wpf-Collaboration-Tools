<UserControl x:Class="CollaborationTools.SideBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CollaborationTools"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
       <TabControl Width="200" TabStripPlacement="Left">
            <!-- 개인 탭 -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountCircle" Width="24" Height="24" Margin="0,0,10,0" />
                        <TextBlock Text="개인" VerticalAlignment="Center" />
                    </StackPanel>
                </TabItem.Header>
                <StackPanel Margin="10">
                    <TextBlock Text="개인 메뉴" FontWeight="Bold" Margin="0,0,0,10" FontSize="16"/>
                    <ItemsControl ItemsSource="{Binding PersonalMenuList}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Title}" 
                                        Margin="0,5" 
                                        Style="{StaticResource MaterialDesignFlatButton}" 
                                        HorizontalContentAlignment="Left"
                                        Command="{Binding DataContext.MenuClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </TabItem>
            
            <!-- 팀 탭 -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountGroup" Width="24" Height="24" Margin="0,0,10,0" />
                        <TextBlock Text="팀" VerticalAlignment="Center" />
                    </StackPanel>
                </TabItem.Header>
                <StackPanel Margin="10">
                    <TextBlock Text="팀 메뉴" FontWeight="Bold" Margin="0,0,0,10" FontSize="16"/>
                    <ItemsControl ItemsSource="{Binding TeamMenuList}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Title}" 
                                        Margin="0,5" 
                                        Style="{StaticResource MaterialDesignFlatButton}" 
                                        HorizontalContentAlignment="Left"
                                        Command="{Binding DataContext.MenuClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
